<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cn.xhu.biz.dao.MenuDao">
    <insert id="insert">
        insert into menu(parent_id, menu_name, url, perms, menu_type, order_num, remark, router_name, router_path, operator)
        values(#{parentId},#{menuName},#{url},#{perms},#{menuType},#{orderNum},#{remark},#{routerName},#{routerPath}#{operator})
    </insert>
    <update id="update">
        update menu
        <set>
            <if test="parentId!=null">
              parent_id=#{parentId},
            </if>
            <if test="menuName!=null and menuName!=''">
                menu_name=#{menuName},
            </if>
            <if test="url!=null and url!=''">
                url=#{url},
            </if>
            <if test="perms!=null and perms!=''">
                perms=#{perms},
            </if>
            <if test="menuType!=null">
                menu_type=#{menuType},
            </if>
            <if test="orderNum!=null and orderNum!=''">
                order_num=#{orderNum},
            </if>
            <if test="remark!=null and remark!=''">
                remark=#{remark},
            </if>
            <if test="routerName!=null and routerName!=''">
                router_name=#{routerName},
            </if>
            <if test="routerPath!=null and routerPath!=''">
                router_path=#{routerPath},
            </if>
            <if test="operator!=null and operator!=''">
                operator=#{operator},
            </if>
            update_time=now()
        </set>
    </update>
    <delete id="delete">
        update menu set is_delete=1 where id=#{id}
    </delete>
    <delete id="deleRoleMenuByMenuId">
        delete from role_menu where menu_id=#{menuId}
    </delete>
    <select id="queryPageMenus" resultType="cn.xhu.core.pojo.Menu">
        select * from menu
        <where>
            1=1
            <if test="menuName!=null and menuName!=''">
               and menu_name=#{menuName}
            </if>
        </where>
        order by create_time asc
    </select>
    <select id="queryById" resultType="cn.xhu.core.pojo.Menu">
        select * from menu where id=#{id}
    </select>
</mapper>